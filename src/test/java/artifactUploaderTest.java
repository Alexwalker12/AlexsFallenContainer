import org.junit.jupiter.api.Test;

import static org.testng.Assert.assertNotNull;

class artifactUploaderTest {
    @Test
    void javaPropertyFileShouldReturnThreeStrings() {

        //Pass in Java properties,
        //Get back the 3 strings tht match "accessToken", "clientIdentifier" and "userLocal"



        final artifactUploaderTest p = artifactUploader.
        assertNotNull(p);
        assertEquals(expectedProperty, p);
    }
    }

}